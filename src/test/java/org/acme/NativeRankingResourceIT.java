package org.acme;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeRankingResourceIT extends RankingResourceTest {

    // Execute the same tests but in native mode.
}